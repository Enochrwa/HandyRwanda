{
  "name": "backend",
  "private": true,
  "scripts": {
    "lint": "ruff check .",
    "format": "ruff format .",
    "test": "pytest",
    "type-check": "mypy --config-file pyproject.toml ."
  }
}
